<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProjectHeyMobile.Views.Chatpages.ChannelSearchPage">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <SearchBar Placeholder="Search..." TextChanged="SearchBar_TextChanged" />
            <ListView x:Name="CategoriesListView"
                  HasUnevenRows="True" 
                  ItemsSource="{Binding Categories}"
                  SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                  Style="{StaticResource listviewStyle}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout HorizontalOptions="Fill" Padding="5" Orientation="Horizontal">
                                <StackLayout HorizontalOptions="FillAndExpand" Orientation="Vertical" Padding="5">
                                    <StackLayout HorizontalOptions="StartAndExpand" Orientation="Vertical">
                                        <Label Text="{Binding Category.Name}" FontSize="Medium" />
                                        <Label Text="{Binding UsersInCategory, StringFormat='{0} users'}" FontSize="Default" />
                                    </StackLayout>
                                </StackLayout>
                            </StackLayout>
                            <ViewCell.ContextActions>
                                <MenuItem 
                                Text="Join" 
                                Command="{Binding Path=BindingContext.JoinChannelCommand, Source={x:Reference CategoriesListView}}"
                                CommandParameter="{Binding}"/>
                            </ViewCell.ContextActions>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>